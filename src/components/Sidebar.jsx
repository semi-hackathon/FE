import React from 'react';
import styled from 'styled-components';

const Sidebar = () => {
  return <SideWrapper>Sidebar</SideWrapper>;
};

export default Sidebar;

const SideWrapper = styled.div`
  width: 10%;
  height: 100%;
  background-color: blue;
`;
